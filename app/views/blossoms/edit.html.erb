<h1>Edit Blossom</h1>

<%= form_with(model: [@tree, @branch, @blossom], local: true) do |form| %>
  <% if @blossom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blossom.errors.count, "error") %> prohibited this blossom from being saved:</h2>

      <ul>
        <% @blossom.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <!-- Add other fields as needed -->

  <div class="actions">
    <%= form.submit "Update Blossom" %>
  </div>
<% end %>

<%= link_to 'Back', tree_branch_path(@tree, @branch) %>
  <%= button_to 'Delete Blossom', [@tree, @branch, @blossom], method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger' %>


